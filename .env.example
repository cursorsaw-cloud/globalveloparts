# PostgreSQL database connection string
# Required for contact form submissions to be saved
# Vercel Dashboard → Project → Settings → Environment Variables
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: Postgres individual credentials (if not using DATABASE_URL)
# PGHOST=
# PGPORT=5432
# PGUSER=
# PGPASSWORD=
# PGDATABASE=
